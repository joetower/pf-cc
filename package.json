{
  "scripts": {
    "start": "lite-server",
    "dev": "npm run start && npx @tailwindcss/cli -i ./src/main.css -o ./src/main-compiled.css --watch",
    "build": "npx @tailwindcss/cli -i ./src/main.css -o ./src/main-compiled.css"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.1.7",
    "tailwindcss": "^4.1.7"
  },
  "devDependencies": {
    "lite-server": "^2.6.1"
  }
}
